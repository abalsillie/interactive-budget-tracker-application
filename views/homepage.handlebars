{{!-- homepage landing: if the user is logged, they get bar graph with data --}}
{{#if loggedIn}}
{{> homepage-graph}}

{{!-- homepage landing: if the user is not logged, they get generic page asking to sign up/sing in --}}
{{else }}
{{> login}}
{{/if }}

<main class="container mt-5">
    <h1 class="mb-4 text-white">Budget Dashboard</h1>
    <div class="row">
        <div class="col-md-5">
            <div class="card mb-3">
                <div class="card-header bg-info">
                    <button class="expenses-button" type="button">
                        <h2>Enter Expenses</h2>
                    </button>
                    <button class="goals-button" type="button">
                        <h2>Your Goals</h2>
                    </button>
                </div>
            </div>
        </div>
    </div>
</main>

<!-- need to enter div for chart.js 3rd party library once established -->